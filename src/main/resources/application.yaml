spring:
  jackson:
    time-zone: UTC
  application:
    name: polio
  datasource:
    url: jdbc:mysql://localhost:3306/polio?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: polio
    password: polio
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 600000       # 10분
      connection-timeout: 30000  # 30초
      max-lifetime: 1800000      # 30분
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8089/realms/polio
      client:
        registration:
          keycloak:
            client-id: polio-toy-client
            client-secret: I6uiQiYIegQVvvIsAgFUpexpgNjfR86H
            scope: openid
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            authorization-grant-type: authorization_code
  mail:
    host: smtp.gmail.com
    port: 587
    username: tutudtutu4@gmail.com         # Gmail 계정
    password: uidt oryy yoqr ygzf       # 앱 비밀번호 (일반 비밀번호 아님)
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

keycloak:
  client-id: polio-toy-client
  client-secret: I6uiQiYIegQVvvIsAgFUpexpgNjfR86H
  realm: polio
  username: polio-admin
  password: polio-admin
  server-url: http://localhost:8089
  token-refresh-threshold-seconds: 60 # access token 만료까지 남은 시간 기준 (초 단위)

cloud:
  aws:
    s3:
      endpoint: https://d3v-okd-s3.ikoob.co.kr
      bucket: proxy-test
    credentials:
      access-key: ${AWS_ACCESS_KEY_ID:proxy-test-api}
      secret-key: ${AWS_SECRET_ACCESS_KEY:proxy-test-api}
    region:
      static: ap-northeast-2
      auto: false